# Feedback Development

- [] Move chatwidget to plugin area instead of different apps

- [] Use Django Task and Postgres as background service instead of Celery. Remove all celery stuff, again using django task, default django feature of tasks
